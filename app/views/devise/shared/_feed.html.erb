<% if @feed_items.any? %>
<ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
    </ol>
    <%= will_partial @feed_items, renderer: BootstrapPagination::Rails %>
    <% end %>